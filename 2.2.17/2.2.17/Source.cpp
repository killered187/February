#include <iostream>
using namespace std;
int main() {
	int input;
	cout << "What song do you want played?" << endl;
	cin >> input;
	if (input == 1)
		cout << "And I know when that hotline bling. It can only mean one thing." << endl;
	else if (input == 2)
		cout << "We mash up the place turn up the place and make them all have fun.!" << endl;
	if (input == 3)
		cout << "Cut my life into pieces. This is my last resort." << endl;
}